<h1>Listing users</h1>

<p><%= link_to "Back to root of web application", root_path %></p>

<table class="table table-bordered table-hover">

  <tr>
    <th>Name</th>
    <th>Lbs email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.lbs_email %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
<p><%= link_to "Home", root_path %></li>
